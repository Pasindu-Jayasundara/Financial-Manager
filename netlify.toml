[build]
  # This repository contains separate frontend and backend projects.
  base = "client"
  command = "npm run build"
  publish = "dist"

# Serve the React app for direct visits to client-side routes.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
